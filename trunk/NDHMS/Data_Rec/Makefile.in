# Makefile.in

CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
NETCDF4_CFLAGS = @NETCDF4_CFLAGS@

MPIFC = @MPIFC@
FCFLAGS = @FCFLAGS@
NETCDF4_FFLAGS = @NETCDF4_FFLAGS@

include ../global.mk

all: libData_Rec.a

libData_Rec.a: $(Data_Rec_objects)
	ar -r $@ $?

$(Data_Rec_objects): %.o: %.f
	$(MPIFC) -o $@ $(FCFLAGS) $(NETCDF4_FFLAGS) -I../MPP $<

%.f: %.F
	$(CPP) $(CPPFLAGS) $(NETCDF4_CFLAGS) $< > $@

clean:
	rm -f *.o *.mod *.stb *~ *.f libData_Rec.a
